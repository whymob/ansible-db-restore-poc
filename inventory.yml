all:
  children:
    sqlserver:
      hosts:
        sqlserver01:
          ansible_host: 10.0.0.10
          ansible_user: admin
    oracle:
      hosts:
        oracle01:
          ansible_host: 10.0.0.11
          ansible_user: oracle
    postgresql:
      hosts:
        postgresql01:
          ansible_host: 10.0.0.12
          ansible_user: postgres